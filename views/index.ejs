<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>user registration form</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>

  <div class="container">
    <h1>User Registration Form</h1>
    <form action="/register" method="post" enctype="multipart/form-data">
      <div class="form-div">

        <div class="rows">
          <h2> Name: </h2>
          <div><input type="text" placeholder="Enter Your Name" class="input-div" name="fullname"></div>
        </div>
        <div class="rows">
          <h2>Username:</h2>
          <div><input type="text" placeholder="Enter your username" class="input-div" name="username"></div>
        </div>
        <div class="rows">
          <h2>Password:</h2>
          <div><input type="password" placeholder="Enter your password" class="input-div" name="password"></div>
        </div>
        
      </div>
      <div class="rows">
        <input type="file" name="uploadd" id="uploadd" >
        <div class="img">

        </div>
      </div>
      <div class="btn-div">
        <input type="submit" value="submit" class="btn">
      </div>
    </form>
  </div>
<script>
  let inputdiv=document.querySelector('#uploadd');
  inputdiv.addEventListener("change",()=>{
    const image = inputdiv.files;
    let thisDiv='';
    Array.from(image).forEach(file => {
            thisDiv += `<img src=${URL.createObjectURL(file)} "></img>`;
        });
        let logoDiv = document.querySelector('.img');
        logoDiv.innerHTML = thisDiv;
        console.log(unilogoDiv);
  })

</script>
</body>

</html>